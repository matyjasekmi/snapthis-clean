<!DOCTYPE html>
<html>
<head><meta charset="utf-8"/><meta name="viewport" content="width=device-width, initial-scale=1" /><title>Checkout success</title></head>
<body>
  <main>
    <h1>Payment successful</h1>
    <p>Dziękujemy! Twoja płatność została przyjęta.</p>
    <% if (typeof token !== 'undefined' && token) { %>
      <p><a href="/guest/<%= token %>">Przejdź do zamówionej strony</a></p>
    <% } else { %>
      <p><a href="/">Return home</a></p>
    <% } %>
  </main>
</body>
</html>
